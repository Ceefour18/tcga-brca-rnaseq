# ---- Data: raw downloads (large, regenerable, not ours to host) ----
data/raw/
GDCdata/
MANIFEST.txt

# ---- Large intermediate object (726 MB) — regenerated by script 01; ----
# ---- the report does not read it, so it must not be committed.        ----
data/processed/brca_se_raw_annotated.rds

# NOTE: the rest of data/processed/ IS committed (dds + results objects,
# all under 35 MB) so the report renders from a fresh clone.

# ---- R clutter ----
.Rproj.user/
.Rhistory
.RData
.Ruserdata
Rplots.pdf
*.Rproj

# ---- Quarto build artifacts ----
report_files/
/.quarto/
# report.html is intentionally NOT ignored — it's the reviewer-facing deliverable

# ---- Editor / OS ----
.vscode/
.DS_Store
Thumbs.db
